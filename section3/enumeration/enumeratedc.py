for index, character in enumerate("abcdefghijklmnopqrstuvwxyz"):
    print(index, character)
    

# the enumerate function takes in two parameters
# takes the index and character
# efficient way to get indexes and print values in a loop

dek = ["A", "B", "C", "D", "E", "F"]

for index, character in enumerate(dek):
    print(index, character)